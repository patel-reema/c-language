#include <stdio.h>
#include <conio.h>

main()
{
	int a,b,c;
	clrscr();

	printf("Enter any number: ");
	scanf("%d",&a);
	printf("Enter any number: ");
	scanf("%d",&b);
	printf("Enter any number: ");
	scanf("%d",&c);

	(a<b)
	 ?printf("%d is minimum.",a)
	 :printf("%d is minimum.",b);

	(b<c)
	 ?printf("%d is minimum.",c)
	 :printf("%d is minimum.",b);

	(c<a)
	 ?printf("%d is minimum.",a)
	 :printf("%d is minimum.",c);

	getch();


}